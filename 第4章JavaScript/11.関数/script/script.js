//課題1
function showAlert() {
  alert("アクションが実行されました")
}
showAlert()

//課題2
var getCalc = function () {
  return 1+1
};
console.log(getCalc());

/*課題3
スコープとは変数の名前や関数などの参照できる範囲を決めるもの。 
スコープの中で定義された変数はスコープの内側でのみ参照でき、スコープの外側からは参照できなくなる。*/
